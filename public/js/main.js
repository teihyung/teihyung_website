window.addEventListener('load', function() {
  setTimeout(function() {
    document.querySelector('.Home_Container').classList.add('visible');
  }, 400); // The delay before the class is added, in milliseconds
});

window.addEventListener('load', function() {
  setTimeout(function() {
    document.querySelector('.email').classList.add('visible');
  }, 600); // The delay before the class is added, in milliseconds
});

window.addEventListener('load', function() {
  setTimeout(function() {
    document.querySelector('.languages').classList.add('visible');
  }, 400); // The delay before the class is added, in milliseconds
});

window.addEventListener('load', function() {
  setTimeout(function() {
    document.querySelector('.interest').classList.add('visible');
  }, 600); // The delay before the class is added, in milliseconds
});

window.addEventListener('load', function() {
  setTimeout(function() {
    document.querySelector('.used_skill').classList.add('visible');
  }, 400); // The delay before the class is added, in milliseconds
});

// const toggleBtn = document.querySelector('.navbar__toggleBtn');
// const menu = document.querySelector('.navbar__menu');
// const icons = document.querySelector('.navbar__icons');
//
// toggleBtn.addEventListener('click', () => {
//   menu.classList.toggle('active');
//   icons.classList.toggle('active');
// });


